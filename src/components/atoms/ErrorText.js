import styled from "styled-components";

const ErrorText = styled.p`
  text-align: right;
  align-items: center;
  color: red;
  font-weight: bold;
  margin-right: 20px;
`;

export default ErrorText;
